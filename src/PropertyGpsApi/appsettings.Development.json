{
  "_comment": "Development overrides only. Base values live in appsettings.json; local credentials belong in appsettings.Local.json, which is git-ignored and layered on top of this.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "PropertyGpsApi": "Debug"
    }
  },
  "Otp": {
    "Sender": "Development"
  }
}
